# DamDa 감사 후속 수정

2026-09-21 · AUDIT_FINAL.md의 확인된 문제에 대한 작업 기록

## 반영 범위

- 공유 시작 시 계정과 snapshot을 고정하고, 저장 대기 중 계정이 바뀌면 공유 INSERT를 중단한다. 이전 계정의 진행 중 저장도 새 계정의 저장 상태와 분리한다.
- 사진 디코딩 완료 시 시작 당시 프로필 객체를 확인하여 다른 계정에 사진을 저장하지 않는다.
- 체험에서 가입 화면으로 이동할 때 즉시 렌더와 hashchange 렌더가 겹쳐 입력한 이름을 지우던 문제를 수정했다. 브라우저 회귀는 제출 직전 이름 보존을 확인한다.
- 체험 공유는 가입 화면으로 연결하며, 체험 프로필·명함함을 가입한 계정으로 옮긴다. 새 공유 링크는 서버에 저장한 사본만 사용한다.
- 저장 실패·충돌 시 백업 또는 미저장 변경을 버리고 로그아웃하는 선택을 제공한다.
- 공유 화면 재진입 시 다시 조회하고, 조회 실패 화면에 재시도 버튼을 제공한다. 공유 종료 시 같은 화면의 캐시도 무효화한다.
- 프로필 입력은 프로필만 직렬화·전송하며, 계정 저장은 변경된 필드만 PATCH한다.
- 현행 UI에 맞게 브라우저 테스트 선택자를 갱신하고 사진 계정 전환, 체험 가입 이관, 공유 재진입·재시도, 로그아웃 복구 회귀를 추가한다.
- DB 검증은 모든 migration과 권한 복구의 반복 적용, INSERT RETURNING 및 사용자 간 격리를 검사한다.
- 과거 로컬 서버에는 필요한 과거 앱 스크립트·스타일을 연결한다. 운영 빌드는 필요한 자산만 복사하며 미사용 cloud-auth.js와 교체되던 함수 본문을 제거한다.
- 뒷면 PNG의 줄무늬·동심원 패턴을 화면과 같은 방향·간격으로 그린다.
- 브라우저 시작 실패 시 서버를 정리하고, 테스트 종료 시 기존 연결 설정으로 다시 빌드한다. 스크린샷은 실행별 디렉터리에 저장하며 DAMDA_TEST_ARTIFACT_DIR로 위치를 지정할 수 있다.

## 검증

- JavaScript 문법 검사, 모델·계정 회귀, 로컬 DB 및 legacy 테스트 통과.
- 390px 모바일 및 1366px 데스크톱 스크린샷 확인.
- 전체 브라우저 통합 검증 통과: 실제 Supabase SDK + 모의 API, 가입·로그인·복구, 저장 실패·충돌, 사진 계정 격리, 체험 가입·이관·공유, 공유 재진입·재시도·종료, PNG/VCF, 320–1920px 화면.
- 테스트 종료 후 실제 config/supabase.json의 연결 설정으로 빌드가 복원됨을 확인했다.

## 범위 밖

운영 Supabase 설정 변경이나 배포는 하지 않았다. 보고서의 조건부 항목인 공유 목록의 페이지 제한 및 서버 공유 총량 제한은 이번 수정에 포함하지 않았다.
