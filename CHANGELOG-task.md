# DamDa 작업 반영 결과

## 완료
- eval / Function 생성자 사용 점검: 앱 소스에서 발견되지 않음. 서버 CSP에서 동적 코드 실행을 허용하지 않음.
- 공개 정보 프리셋 선택·추가·이름 변경·현재 선택으로 덮어쓰기·삭제.
- 이름/닉네임 선택과 공개 여부. 공유 사본에 선택한 이름만 포함.
- 프로필 사진 원형/둥근 사각형/사각형, 작게/보통/크게. 공유 화면과 PNG에도 반영.
- 모바일 입력 글자 크기, 버튼 터치 영역, 좁은 화면 줄바꿈 개선.
- QR 링크로 받은 명함을 명함함에 저장. 동일 공유 명함 중복 저장 방지.
- 명함함 분류 탭 생성·이름 변경·삭제·명함 이동. 분류 삭제 시 미분류로 이동.
- 내 명함 저장 후 기기 공유 또는 링크 복사.
- 비로그인 상태의 저장 요청 보관. 체험 가입 완료 또는 로그인 후 자동 저장.
- 시스템/라이트/다크 화면 모드 및 한국어/영어 설정.
- DamDa 명칭과 제공된 로고 적용. 브라우저 아이콘, 공유 제목, PNG/VCF/QR 다운로드 파일명 변경.

## 디자인 목업 — 승인 후 실제 앱에 적용 완료
- `dist/mockup.html`: 내 명함, 명함함, 프로필 사진 편집, 이름 선택, 공개 항목 프리셋.
- 모바일 보기 버튼, 다크/라이트 전환, 예시 명함 분류 필터.
- 목업에서 변경하는 정보는 저장하거나 외부로 전송하지 않음.
- 사용자 요청으로 전체 화면 구조와 프로필 사진 편집 영역 재구성을 실제 앱에 적용 완료.
- 사이드 메뉴·실제 저장 명함 목록·프리셋·사진 편집·다크모드·언어 설정을 새 레이아웃에 연결.
- 상단 저장·공유 버튼이 공유 사본과 명함함 저장을 함께 수행.
- 담다 블루 테마를 공유 API와 PNG 다운로드에도 반영.

## 보류
- OCR로 명함 추가: 사용자 요청으로 이번 구현에서 제외. 관련 코드와 의존성 제거.

## 저장 및 인증 범위
- 기존 체험 인증 구조 유지. 실제 회원 계정/비밀번호 서버는 없음.
- 명함함과 사용자 프리셋은 이 브라우저의 localStorage에 저장됨. 계정 간 분리나 기기 간 동기화 기능은 아님.
- 가입 전 저장 대기는 sessionStorage에 저장되며 현재 탭에서 이어짐.
- 기존 프로필 유지용 fold-* 저장 키는 기존 사용자의 데이터를 유지하기 위해 보존.
- 사진이 큰 명함을 많이 저장하면 브라우저 용량 제한에 도달할 수 있으며 실패 안내를 표시함.
- 공유는 기존과 동일하게 실행 중인 서버와 같은 Wi-Fi가 필요.

## 검증
- `npm run check`
- `npm test`: 기존 로그인·공유 API·QR·파일 유지·내보내기 검증 및 신규 기능 회귀 검증.
- 목업의 로컬 자산 참조, 제공 로고 응답, 서버 CSP 검증.
- 실제 휴대폰 QR 스캔, 기기 공유 메뉴, 브라우저 렌더링 시각 검사는 미실시.


## 프리셋 재수정 및 애니메이션
- 접힌 관리 메뉴 대신 프리셋 추가 버튼과 각 항목의 편집·삭제 버튼을 직접 노출.
- 독립 편집 폼에서 이름, 공개 항목, 이름/닉네임, 이름 공개 여부를 저장하고 적용.
- 선택한 프리셋을 프로필과 함께 유지하고 변경 상태 표시. 기존 프리셋과 명함 데이터 유지.
- 로고 호버, 버튼 누르기, 메뉴 이동, 명함 호버, 화면 진입 효과 추가.
- 화면 상단 애니메이션 설정에서 시스템 설정 또는 애니메이션 줄이기 선택. 브라우저에 설정 저장. OS가 동작 줄이기를 요청하면 항상 효과 억제.
- 자동 검증: 실제 이벤트 핸들러를 통한 프리셋 선택·추가·편집·삭제·취소, 이름과 공개 항목 저장, 선택 복원, OS 및 앱 동작 줄이기 설정.
